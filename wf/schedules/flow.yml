dependencies:
  - name: full_load
    tasks:
      - prepare_data
      - transform
      - calculate_kpis
    schedule: "0 5 * * *"  # 5 AM
    
  - name: incremental
    tasks:
      - prepare_data
      - calculate_kpis
    schedule: "0 */2 * * *"  # Every 2 hours