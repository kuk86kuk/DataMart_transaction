select
    "123" as customer_id,
    "COUNT(*)" as tx_count,
    "SUM(amount)" as total_amount,
    "AVG(amount)" as avg_amount,
    "123" as customer_id_test,
    "COUNT(*)" as tx_count_test,
    "SUM(amount)" as total_amount_test,
    "AVG(amount)" as _test  
    
